# ===============================
# ⚙️ إعدادات قاعدة البيانات
# Database Configuration
# ===============================
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=nfc_discount_system

# ===============================
# 🔐 إعدادات الأمان
# Security Configuration
# ===============================

# مفتاح JWT السري (استخدم قيمة قوية جداً - على الأقل 32 حرف)
# Generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_minimum_32_characters

# 🔴 مفتاح توقيع NFC (مطلوب - على الأقل 32 حرف)
# ⚠️ CRITICAL: This key is used to sign NFC cards. Keep it secret!
# Generate one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NFC_SIGNATURE_SECRET=your_nfc_signature_secret_minimum_32_characters

# مفتاح التشفير الرئيسي للشركة
COMPANY_MASTER_KEY=enterprise-secure-master-key-2024

# رمز الشركة للتحقق
COMPANY_TOKEN=ANTIGRAVITY-SECURE-ID-2024

# ===============================
# 🌍 البيئة والتطوير
# Environment & Development
# ===============================

# وضع البيئة: development, testing, production
NODE_ENV=development

# السماح بإنشاء حساب إدارة تلقائياً (تطوير فقط)
ALLOW_AUTO_ADMIN=false

# فعّل وضع التطوير
DEBUG_MODE=false

# ===============================
# 📡 إعدادات Supabase
# Supabase Configuration
# ===============================

# عنوان Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# مفتاح Supabase العام
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# مفتاح Supabase الخاص (الخادم فقط)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===============================
# 🎴 إعدادات قارئ NFC
# NFC Bridge Configuration
# ===============================

# معرف المحطة الافتراضي
TERMINAL_ID=1

# عنوان اتصال المحطة
NEXT_PUBLIC_TERMINAL_URL=http://localhost:3000

# WebSocket للاتصالات المحلية (قديم - محلي فقط)
WS_PORT=8999

# ===============================
# 📊 إعدادات المراقبة
# Monitoring Configuration
# ===============================

# فعّل مراقبة الأجهزة
ENABLE_DEVICE_MONITORING=true

# فترة فحص الصحة (بالميلي ثانية)
HEALTH_CHECK_INTERVAL=30000

# مهلة عدم النشاط (بالميلي ثانية)
INACTIVITY_TIMEOUT=300000

# ===============================
# ⚡ إعدادات الأداء
# Performance Configuration
# ===============================

# فترة المسح (بالميلي ثانية)
POLLING_INTERVAL=100

# أقصى عدد مسحات متزامنة
MAX_CONCURRENT_SCANS=5

# مهلة زمنية للمسح (بالميلي ثانية)
SCAN_TIMEOUT=5000

# مدة الـ cache (بالميلي ثانية)
CACHE_DURATION=10000

# ===============================
# 📝 إعدادات السجلات
# Logging Configuration
# ===============================

# مستوى السجل: ERROR, WARN, INFO, DEBUG
LOG_LEVEL=INFO

# نوع السجل: console, file, both
LOG_TYPE=both

# مسار ملف السجل
LOG_PATH=./logs

# أقصى حجم لملف السجل (بالميجابايت)
LOG_MAX_SIZE=50

# عدد أيام الاحتفاظ بالسجلات
LOG_RETENTION_DAYS=30

# Supabase Configuration (Phase 4 - Global Scalability)
# Get these from your Supabase Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# NFC Reader Configuration
# Terminal and Branch IDs for the NFC reader
TERMINAL_ID=1
BRANCH_ID=1

# Scan Events Cleanup
# How long to keep scan events in the database (in hours)
SCAN_EVENTS_RETENTION_HOURS=24
